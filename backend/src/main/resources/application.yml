server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:bakerydb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  cors:
    allowed-origins: http://localhost:4200
  jwt:
    secret: "bakery-dev-secret-key-change-me-before-production-0123456789"
    expiration-ms: 86400000
  ocr:
    tessdata-path: "C:/Users/Public.DESKTOP-3A1LBHM/AppData/Local/Programs/Tesseract-OCR/tessdata"
