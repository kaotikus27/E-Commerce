# Backlog

Open, not-yet-scheduled work. Carried forward across sessions — check off and move to `handoff.md`/`DECISIONS.md` once actually picked up.

- [x] ~~Merge the FAQ and Contact Us pages into one, per `contact-us-page.png` mockup~~ Built 2026-08-18: `/faq` now redirects to `/contact` (nav link removed, `faq-page.component.ts` deleted as dead code — `/admin/faqs` content-management is untouched, still feeds the accordion). New `ContactPageComponent`: a real contact form ("Send Us a Message" — name/email/phone-optional/topic/message) backed by a brand-new `ContactMessage` backend entity + public submit endpoint + admin inbox at `/admin/messages` (list, mark read/unread, delete — no edit, these are customer-submitted). "Find Us" card uses real `StoreService` data (address, today's hours, phone) plus a real embedded OpenStreetMap iframe and a real Waze deep link — both newly derived from `storeLatitude`/`storeLongitude`, which existed server-side already but weren't exposed on the public `GET /api/v1/store` DTO until now. FAQ accordion reuses the existing real `FaqAccordionComponent`/`FaqService` unchanged. Ghibli-mockup naming genericized per the established convention (e.g. "Send a Messenger Sprite" → "Get in Touch", "Chihiro Ogino" placeholder → "Juan Dela Cruz"). **Verified end-to-end via direct API calls**: contact-form submission, admin list/mark-read/delete lifecycle, unauthenticated admin access correctly rejected (403), and the new `latitude`/`longitude` fields confirmed present on `GET /api/v1/store`. **Known limitation, same as promo codes**: live visual verification of both the new `/contact` page and `/admin/messages` in the browser hit the same already-tracked Open Issue 12 symptom (routed content not mounting after navigation) — confirmed the component's own data layer is 100% correct via `ng.getComponent()` (real store address/hours were already resolved, just not painted to the DOM), so this is the pre-existing app-wide bug, not a defect in this feature. Test contact-message rows created during verification were deleted afterward.
- [x] ~~No priced customization modifiers for products~~ Admin can now set a per-option surcharge (e.g. Oat milk +₱20) per product; priced server-side at order placement (DEC-012, 2026-08-15).
- [ ] No dedicated refund UI for `PaymentStatus.REFUNDED`.
- [ ] No phone number field on the customer order-tracking page.
- [ ] Category restructure (4 categories → 3) — still undecided.
- [ ] Wallet-balance visibility in admin — the `WALLET_BALANCE_CHANGED` webhook is received but not persisted or shown anywhere.
- [ ] Lalamove cancel-and-clone order replacement (`ORDER_REPLACED` event) not handled — `applyDeliveryWebhookUpdate()` looks up strictly by the original `lalamoveOrderId`, so a replaced order's updates would silently stop applying.
- [ ] No way to check Lalamove wallet balance before dispatch — a dispatch can fail with a real `402 insufficient credit` with no advance warning.
- [ ] Sales Summary admin page — planned, not yet built.
- [ ] Satellite-view toggle (carried over from an earlier session, still untouched).
- [ ] Build Rail roadmap artifact update (carried over from an earlier session, still untouched).
- [ ] `lalamoveOrderId` isn't exposed by any admin API/UI — the only way to look it up today is a raw H2-console SQL query (see `backend/scripts/simulate-lalamove-webhook.js` header for the exact steps). Worth surfacing on the admin order view.
- [ ] No cron/background auto-poll for orders stuck in `ASSIGNING_DRIVER` beyond N minutes — DEC-006 only covers manual/on-demand sync.
- [ ] Cloudflare quick tunnel (`*.trycloudflare.com`) for the Lalamove webhook is not durable — its URL changes every restart and must be re-registered in the Lalamove sandbox dashboard each time (see `docs/lalamove-webhook-tunnel.md`). A named Cloudflare tunnel would fix this permanently but isn't set up yet.
- [x] ~~`order.status` (customer tracking stepper) and `deliveryStatus` (Lalamove rider lifecycle) have no auto-sync~~ Auto-advance wired in `syncOrderStatusFromDelivery()` (DEC-010, 2026-08-15).
- [x] ~~Stepper still isn't delivery-aware~~ Delivery orders now get their own 5-step sequence reflecting Lalamove's real lifecycle (DEC-011, 2026-08-15).
- [x] ~~`OrderRepository.existsByGcashReference` (added in `f1eb8c7`) is unused~~ Wired into `placeOrder` (DEC-008, 2026-08-15).
- [ ] Home page "Our Philosophy" section (DEC-015) image is a temporary stock placeholder (DEC-023, 2026-08-17) — still needs Leo's real photo/illustration asset to replace it permanently.
- [x] ~~Home page section width/margin consistency check against the mockup~~ Welcome and Promo banner were narrower than the mockup intended — fixed to inherit the shared container width (DEC-024, 2026-08-17). FAQ (no mockup counterpart) and Visit Us (mockup uses a different, genuinely narrower single-column layout) deliberately left as-is per Leo's call.
- [x] ~~Testimonials section (Ghibli redesign) still uses placeholder names copied from actual Studio Ghibli characters (Sophie Hatter, Chihiro Ogino)~~ Restyled using the existing realistic customer names instead — no copyrighted names used (DEC-018, 2026-08-15).
- [x] ~~Seeded "Sourdough Loaf" product has no `image` value~~ Root cause was actually a dead Unsplash photo ID (404), not a missing value. Replaced with a verified working free-license photo and pushed live via the admin API (DEC-022, 2026-08-17).
- [ ] No auto-logout / re-login prompt when the stored admin JWT expires (24h lifetime). `auth.interceptor.ts` only attaches the token to outgoing requests — nothing catches a `401`/`403` response to clear the stale token or redirect to `/admin/login`, so an expired session just shows silent `403`s on every admin API call until the admin manually logs out and back in. Found 2026-08-17 while investigating a reported `403` on the admin panel (root cause there was simply a stale token from a prior session — logging out/in fixed it).
- [x] ~~Checkout page redesign~~ Phases 1–3 done (2026-08-17): visual restyle, sticky 2-column layout, and structured PH address fields (Option B — concatenated into the existing free-text geocoding search string, no backend change). See `docs/checkout-redesign-notes.md`. Phase 4 (promo codes) tracked as its own item below.
- [x] ~~Redeemable promo/discount codes at checkout (e.g. "CHILL10")~~ Built 2026-08-17 (checkout redesign Phase 4): new `PromoCode` entity/table (code, PERCENT or FIXED discount, active flag), a public `/api/v1/promo-codes/validate` preview endpoint, and admin CRUD at `/admin/promo-codes` (mirrors the Promotions admin page). Checkout page gets an Apply/Remove promo input in the Order Review sidebar; the discount is re-derived reactively client-side for the preview but resolved authoritatively server-side again at `placeOrder()` — never trusted from the client, same principle as customization surcharges (DEC-012). Seeded the `CHILL10` (10%) code the homepage promo banner already advertised. **Verified live end-to-end via direct API calls** (validate, full CRUD lifecycle, and a real placed order: ₱300 subtotal → 10% off → ₱270 discounted subtotal → ₱23.63 tax → ₱293.63 total, test order cancelled after) — matches the frontend's own computed preview exactly. **Along the way, found and fixed a real, unrelated, currently-live bug**: the `order_items.gift_wrap` column (added for DEC-028's gift-wrap feature) was silently missing from the actual H2 table — its NOT-NULL migration had failed on existing rows during a schema-update copy and left the table without the column, breaking **all** order placement with a 500 error. Fixed via a direct `ALTER TABLE ORDER_ITEMS ADD COLUMN GIFT_WRAP BOOLEAN DEFAULT FALSE NOT NULL` (H2 console) — confirmed the column now exists and order placement works again. **Known limitation, not a new bug**: verifying the checkout promo UI and the new admin page live in the browser hit the same already-escalated Open Issue 12 symptom (child route content/post-interaction re-render not mounting on this automated tab) — confirmed identical on the pre-existing `/admin/orders` route too, so it's not specific to this feature. Business logic verified instead via direct signal/API reads, which matched the server exactly.
- [x] ~~Escalated scope on the existing blank-render bug (`docs/docsdebug.md`): admin routes affected too, not just customer-facing static pages~~ **Root-caused and fixed 2026-08-19.** `app.config.ts`'s `provideZoneChangeDetection({ eventCoalescing: true })` was deferring the post-navigation/post-event change-detection flush to a later animation-frame boundary, and that deferred flush could end up scheduled against stale state and never fire — component data was always correct (confirmed via `ng.getComponent()`), only the DOM never painted it. Fixed by setting `eventCoalescing: false` (reverts to Angular's default synchronous CD scheduling). Verified via real `.click()` navigations across `/about`/`/contact`/`/shop`, a hard reload of `/faq`, and the cart-drawer signal case — all now paint immediately, no `ng.applyChanges()` workaround needed. See DEC-039 and `docs/docsdebug.md` for the full investigation.
- [x] ~~Hard-loading or refreshing `/checkout` directly always redirects back to `/shop`~~ Fixed 2026-08-17: added `StoreService.ensureLoaded()` (resolves once the first store-info fetch completes, or after a 5s safety timeout), and `checkoutGuard` now awaits it before reading `store.isOpen()`. Verified live: a hard `location.href` load of `/checkout` with a seeded cart now renders the page instead of bouncing to `/shop`; an empty cart still correctly redirects.
