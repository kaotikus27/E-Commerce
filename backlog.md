# Backlog

Open, not-yet-scheduled work. Carried forward across sessions — check off and move to `handoff.md`/`DECISIONS.md` once actually picked up.

- [x] ~~No priced customization modifiers for products~~ Admin can now set a per-option surcharge (e.g. Oat milk +₱20) per product; priced server-side at order placement (DEC-012, 2026-08-15).
- [ ] No dedicated refund UI for `PaymentStatus.REFUNDED`.
- [ ] No phone number field on the customer order-tracking page.
- [ ] Category restructure (4 categories → 3) — still undecided.
- [ ] Wallet-balance visibility in admin — the `WALLET_BALANCE_CHANGED` webhook is received but not persisted or shown anywhere.
- [ ] Lalamove cancel-and-clone order replacement (`ORDER_REPLACED` event) not handled — `applyDeliveryWebhookUpdate()` looks up strictly by the original `lalamoveOrderId`, so a replaced order's updates would silently stop applying.
- [ ] No way to check Lalamove wallet balance before dispatch — a dispatch can fail with a real `402 insufficient credit` with no advance warning.
- [ ] Sales Summary admin page — planned, not yet built.
- [ ] Satellite-view toggle (carried over from an earlier session, still untouched).
- [ ] Build Rail roadmap artifact update (carried over from an earlier session, still untouched).
- [ ] `lalamoveOrderId` isn't exposed by any admin API/UI — the only way to look it up today is a raw H2-console SQL query (see `backend/scripts/simulate-lalamove-webhook.js` header for the exact steps). Worth surfacing on the admin order view.
- [ ] No cron/background auto-poll for orders stuck in `ASSIGNING_DRIVER` beyond N minutes — DEC-006 only covers manual/on-demand sync.
- [ ] Cloudflare quick tunnel (`*.trycloudflare.com`) for the Lalamove webhook is not durable — its URL changes every restart and must be re-registered in the Lalamove sandbox dashboard each time (see `docs/lalamove-webhook-tunnel.md`). A named Cloudflare tunnel would fix this permanently but isn't set up yet.
- [x] ~~`order.status` (customer tracking stepper) and `deliveryStatus` (Lalamove rider lifecycle) have no auto-sync~~ Auto-advance wired in `syncOrderStatusFromDelivery()` (DEC-010, 2026-08-15).
- [x] ~~Stepper still isn't delivery-aware~~ Delivery orders now get their own 5-step sequence reflecting Lalamove's real lifecycle (DEC-011, 2026-08-15).
- [x] ~~`OrderRepository.existsByGcashReference` (added in `f1eb8c7`) is unused~~ Wired into `placeOrder` (DEC-008, 2026-08-15).
- [ ] Home page "Our Philosophy" section (DEC-015) image is a temporary stock placeholder (DEC-023, 2026-08-17) — still needs Leo's real photo/illustration asset to replace it permanently.
- [x] ~~Home page section width/margin consistency check against the mockup~~ Welcome and Promo banner were narrower than the mockup intended — fixed to inherit the shared container width (DEC-024, 2026-08-17). FAQ (no mockup counterpart) and Visit Us (mockup uses a different, genuinely narrower single-column layout) deliberately left as-is per Leo's call.
- [x] ~~Testimonials section (Ghibli redesign) still uses placeholder names copied from actual Studio Ghibli characters (Sophie Hatter, Chihiro Ogino)~~ Restyled using the existing realistic customer names instead — no copyrighted names used (DEC-018, 2026-08-15).
- [x] ~~Seeded "Sourdough Loaf" product has no `image` value~~ Root cause was actually a dead Unsplash photo ID (404), not a missing value. Replaced with a verified working free-license photo and pushed live via the admin API (DEC-022, 2026-08-17).
- [ ] No auto-logout / re-login prompt when the stored admin JWT expires (24h lifetime). `auth.interceptor.ts` only attaches the token to outgoing requests — nothing catches a `401`/`403` response to clear the stale token or redirect to `/admin/login`, so an expired session just shows silent `403`s on every admin API call until the admin manually logs out and back in. Found 2026-08-17 while investigating a reported `403` on the admin panel (root cause there was simply a stale token from a prior session — logging out/in fixed it).
- [ ] Checkout page redesign, planned but not started — see `docs/checkout-redesign-notes.md` for the full comparison against `home-cafe-bami-checkout.png` and a UI/UX review. Needs Leo's decision on the address-form approach (free-text search vs. structured PH address fields) before any code is written.
- [ ] Redeemable promo/discount codes at checkout (e.g. "CHILL10") — doesn't exist today; the current `Promotion` model is a homepage marketing banner only, not a customer-entered code with a computed order discount. Surfaced while reviewing the checkout mockup, 2026-08-17.
- [ ] **Escalated scope on the existing blank-render bug (`docs/docsdebug.md`):** confirmed 2026-08-17 that hard-loading an admin route (`/admin/orders`) reproduces the same first-change-detection-pass-never-runs symptom already logged for `/about`/`/contact`/`/faq`/`/terms`/`/shop` — but worse: the admin child route content never mounts at all, `AppComponent`'s own `isAdminRoute()` conditional fails to hide the customer navbar/footer (both render alongside the admin shell), and clicking a sidebar `routerLink` doesn't even navigate. Suggests this is an app-wide Router/Zone/change-detection issue, not isolated to lazy lower-priority pages — worth re-weighing priority given it now plausibly affects admin operations.
- [x] ~~Hard-loading or refreshing `/checkout` directly always redirects back to `/shop`~~ Fixed 2026-08-17: added `StoreService.ensureLoaded()` (resolves once the first store-info fetch completes, or after a 5s safety timeout), and `checkoutGuard` now awaits it before reading `store.isOpen()`. Verified live: a hard `location.href` load of `/checkout` with a seeded cart now renders the page instead of bouncing to `/shop`; an empty cart still correctly redirects.
