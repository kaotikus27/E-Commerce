---
title: "Home by Bami E-Commerce — Handoff"
type: handoff
status: active
owner: "Leo"
created: 2026-08-11
updated: 2026-08-19
ai_access: internal
ai_generated: true
review_status: draft
canonical: true
---

# Current Objective

~~Manually verify the checkout delivery flow end-to-end~~ **Done** (prior session) — verified with `claude-in-chrome` browser automation, cross-checked against actual network responses rather than trusting rendered DOM alone.

**2026-08-13 session:** attempted to reproduce Open Issue 3 (Admin → Store Settings silently fails to render) live — could not reproduce it across ~8 different navigation paths (fresh login, every tab-to-tab transition, modal-open-then-nav, rapid clicking, hard reload). Deferred to when the user is on the Mac machine, where it may be more reproducible or may turn out to already be fixed. Then fixed Open Issues 2 and 4 instead (H2 persistence, OCR path). Then Leo filed a new bug (`docs/docsdebug.md`, "LALA-INTEG-044" — Live Orders stuck at `ASSIGNING_DRIVER`) and wrote `PROPOSAL.md` proposing a fix; reviewed the proposal (agreed with the direction, corrected two things in it), implemented the backend half, then wired up and verified the frontend "Refresh Delivery Status" button live in the real browser — LALA-INTEG-044 is now closed. See DEC-006 / Open Issue 10.

**2026-08-15 session:** Leo had separately pushed `f1eb8c7` (groundwork: added `Order.publicToken` + the order-number collision fix, but deliberately left the public tracking endpoint and frontend still keyed on `orderNumber`). Finished wiring it: public GET now resolves by `publicToken` only, `placeOrder`'s response switched to the redacted `publicView()` (closing a pre-existing leak where the guest checkout response returned `gcashReference`/`receiptImagePath`/`ocrExtractedRef` unredacted), and the frontend (checkout → confirmation → track) now routes/polls by token. See DEC-007. Then also wired up `existsByGcashReference` (also added in `f1eb8c7` but unused) so a GCash order whose OCR-extracted reference was already used by a prior order gets rejected with `409` at placement, instead of silently succeeding. See DEC-008.

**2026-08-15 session (new Claude Code session, follow-up):** picked up an uncommitted, already-finished edit to `backend/scripts/simulate-lalamove-webhook.js` left over from the DEC-007 work above — its `--order-number` flag was now silently 404ing on every `--lifecycle` status print, since the public endpoint resolves by `publicToken` only. Committed as-is (renamed to `--public-token`, header/H2-console docs updated to match), pushed both the submodule (`c7f70fd`) and the vault's pointer bump (`a243ab2`) to `origin/main`.

**2026-08-15 session (continued):** Leo filed ticket ORD-TRACK-089 — customer order-tracking stepper for `ORD-193396` stuck on "Preparing", reported with a hypothesis that a Spring Boot WebSocket/STOMP broadcast was missing. Investigated: confirmed via grep that no WebSocket infrastructure exists anywhere in this codebase (the frontend already REST-polls every 6s by design, per `order-status-page.component.ts`'s own comment), so that hypothesis was a false lead. Queried `ORD-193396` directly via the H2 console (admin API needs auth I didn't have on hand yet at that point) and found the real cause: `order.status` (what the stepper renders) and `deliveryStatus` (Lalamove's rider lifecycle) are independent fields with no auto-sync — the order's rider had already reached `deliveryStatus=PICKED_UP`, but nothing advances `order.status` except an explicit admin action, so it sat at `PREPARING` correctly-but-unhelpfully. Logged the full investigation in `docs/docsdebug.md`. Then looked up the seeded admin credentials (`AdminDataSeeder.java`), logged in via `POST /api/v1/auth/login`, and manually advanced `ORD-193396` from `PREPARING` → `READY` via `PATCH /api/v1/admin/orders/{orderNumber}/status` — verified live via the public tracking endpoint that the customer-facing status now reads `READY`. Deliberately did not jump straight to `COMPLETED`, since `deliveryStatus` is still `PICKED_UP` (rider en route, not yet delivered) — see Open Issue 11.

**2026-08-15 session (continued further):** Leo brought an SSE-based architecture proposal (Spring Boot `SseEmitter` + Angular `EventSource`) for improving how live order updates reach the admin board and customer tracker. Reviewed it, flagged real risks (unredacted PII over an endpoint the plan's own auth workaround would leave unauthenticated, single-instance-only emitter registry, and the fact it wouldn't actually fix ORD-TRACK-089 since that's an `order.status`/`deliveryStatus` sync gap, not a push-latency problem — see Open Issue 11), and recommended the much cheaper RxJS `timer`/`switchMap` polling upgrade instead, given current scale (one admin, dev/sandbox, no production deployment decided). Leo agreed. Implemented across `admin-order.service.ts` (added `fetchOrders()`, an observable-returning twin of `loadOrders()` so a polling timer can compose it with `switchMap`; `loadOrders()` itself untouched for existing fire-and-forget callers), `admin-orders-board.component.ts`, and `order-status-page.component.ts` (added `takeWhile` so polling stops once a terminal status is reached, replacing the old manual `clearInterval`-inside-the-callback approach). Verified live in Chrome on both surfaces — advanced real test orders (`ORD-698781`, `ORD-485158`) via the admin API mid-poll and confirmed both the board and the customer tracker updated without a manual refresh, and confirmed via the network log that polling genuinely stops after a terminal `COMPLETED` status. Test orders reverted to their prior state afterward. See DEC-009.

**2026-08-15 session (continued further still):** Closed Open Issue 11 for real. Added `syncOrderStatusFromDelivery(Order)` in `OrderService.java`, called from inside `applyDeliveryWebhookUpdate()` (the single choke point both the real Lalamove webhook and DEC-006's manual sync button already funnel through) — auto-advances `order.status` to `READY` for delivery orders the moment a driver is assigned or `deliveryStatus` reaches `ON_GOING`/`PICKED_UP` (deliberately the exact same condition `canMarkReady()` already uses to unlock the manual button), and to `COMPLETED` the moment `deliveryStatus` itself reaches `COMPLETED`. Pickup orders and already-terminal orders are untouched; manual "Mark Ready"/"Mark Completed" buttons still work as a fallback. Compiled clean, restarted the backend to pick up the change, then verified live with `simulate-lalamove-webhook.js` against a real `PREPARING` order (`ORD-485158`): a simulated `DRIVER_ASSIGNED` event flipped `order.status` to `READY` immediately, and a simulated `ON_GOING → PICKED_UP → COMPLETED` sequence flipped it through to `COMPLETED` in step. Reverted the order back to its exact pre-test state afterward via a direct H2 update (no API resets `deliveryStatus`/driver fields). See DEC-010.

**2026-08-15 session (continued yet further):** Made the customer order-tracking stepper delivery-aware (backlog follow-up from DEC-010). Delivery orders now get a distinct 5-step sequence (`Order Received → Preparing → Rider Assigned → Picked Up → Delivered`) reflecting Lalamove's real rider lifecycle, instead of relabeling the 4-step pickup sequence; pickup orders are unchanged. Added a separate failure banner for the one case DEC-010 doesn't cover — a Lalamove delivery `REJECTED`/`CANCELED` while `order.status` never got marked `CANCELLED`. No backend changes needed — `deliveryStatus`/`driverName` were already on the public API response, just unused by the stepper. Hit and fixed a real Angular template-compiler bug along the way (`@for` track/`let` syntax), and separately discovered the frontend dev server had stopped picking up file changes entirely (running since the prior day) — restarted it, which is what surfaced the compiler error. Verified live in Chrome: walked a real delivery order through all 5 states via `simulate-lalamove-webhook.js`, simulated a `REJECTED` event on a second order to confirm the failure banner, and placed+walked a fresh synthetic pickup order through its unchanged 4-step path. All test orders reverted/cancelled afterward. See DEC-011.

**2026-08-15 session (continued yet further still):** Picked up the next backlog item — priced customization modifiers. Reviewed the existing system first: customizations are entirely tied to 3 hardcoded presets (Milk/Sugar/Temperature), admin only toggles them on/off via checkboxes, and `lineTotal` was computed purely from base price, ignoring selected options entirely. Chose the bigger of two options offered (admin-editable per-option pricing, not hardcoded deltas) and implemented it: admin form now shows a price input per option under each checked customization box (defaulting to ₱0, no assumed prices); pricing is resolved authoritatively server-side in a new `OrderService.resolveOptionsSurcharge()` at order-placement time, which also now validates a selected option is real for that product (400 if not) — necessary now that an invalid selection would otherwise silently price at zero. `Customization.optionsCsv`'s format changed to embed price (`"Oat:20"` instead of `"Oat"`), with backward-compatible decoding for existing rows. Declined an itemized cart/receipt price breakdown (offered, not wanted — correct totals are enough). Verified live in Chrome + direct API against a real product (Iced Latte): set Oat +₱20/Almond +₱25 via the admin form, confirmed the customer dropdown/Add-to-Cart total reflect it, confirmed a placed order's actual `unitPrice`/`lineTotal` include the surcharge, and confirmed a forged option name is rejected with 400. Along the way, hit a real process gotcha (not a code bug): the running backend was serving stale bytecode from before this feature's changes — `mvnw compile` alone doesn't restart an already-running `spring-boot:run` process, so the first save attempt silently persisted zero prices using the old DTO shape. Restarting the backend resolved it. All test data reverted/cancelled afterward — Iced Latte's customization prices back to 0, both synthetic test orders cancelled. See DEC-012.

**2026-08-15 session (continued, new UI redesign):** Leo shared a full Ghibli-themed home page redesign mockup (`frontend/src/assets/home-cafe-by-bami-landing.png`) plus a real hero video asset (`homepage_hero_video.mp4`), wanting to rebuild the home page section-by-section, starting with the hero. Scoped it first: confirmed the video fully replaces the old static hero image (no separate illustration cutout needed), and that testimonial placeholder names in the mockup (actual Studio Ghibli character names — Sophie Hatter, Chihiro Ogino) get swapped to originals when that section is built later, using them temporarily for now. Rebuilt the hero: video-based, click-to-play + autoplay/muted/loop, a mute toggle, live `StoreService`-bound address/hours overlay (not the mockup's hardcoded fictional hours), and the "Order Now" CTA. Spent most of the effort on the source video's baked-in AI-generation watermark — tried a solid corner patch first (Leo correctly called this out as looking like an obvious cover-up), then crop/zoom + vignette, then landed on an enlarged/repositioned "Order Now" button doing double duty as both a real CTA and the cover, confirmed via Leo's own screenshots (the mark turned out to be a small gray triangle, not initially visible to either of us without a direct screenshot). See DEC-013 for the full technical writeup. Separately hit and diagnosed a real environment quirk (not a code bug): the video file doesn't render in this machine's Chrome-automation test tab at all (stuck at `readyState 0` even navigating directly to the raw file URL, bypassing the app) — file itself checked out fine server-side (valid range-request support, standard H.264 container) — Leo can see it fine in his own browser, so this is specific to the automated test tab, not a real defect.

**2026-08-15 session (continued, redesign section 2):** Built the "Welcome to Home Cafe by Bami" section per the mockup — eyebrow, heading, copy, and two CTAs ("Explore Our Menu" anchor-scrolls to the existing `#discover` section, "Our Philosophy" points to `#philosophy` which doesn't exist yet). Retired the old `.hero-bar` (Order Now/View Cart/Discover the Flavors) that used to sit under the hero — confirmed "View Cart" was redundant since the persistent header nav bar already has its own cart icon opening the same drawer. Verified live in Chrome. See DEC-014.

**2026-08-15 session (continued, after a power-outage gap — new Claude Code session):** Picked up an in-progress, uncommitted edit left in the working tree from just before the outage: the "Our Philosophy" section (redesign section 3), already written but never verified or committed. Restarted both dev servers (both had gone down in the outage), verified live in Chrome — section renders correctly with its image placeholder (mockup has no image in that slot either, not a missed asset), no console errors, and confirmed the Welcome section's "Our Philosophy" link (DEC-014) now correctly anchor-scrolls to it. Committed as DEC-015. See `backlog.md` for the two follow-up gaps flagged (Philosophy image asset, testimonial placeholder names).

**2026-08-15 session (continued, redesign section 4 — menu preview):** Redesigned "Discover the Flavors" to match the mockup's "What's Brewing Today" section — copy/button text updated, 3→4 featured products, 3→4 desktop grid columns. Reused the existing `app-product-card` component and `.grid-responsive` layout as-is (it's shared with `/shop`, out of scope to redesign). Verified live in Chrome: correct copy, 4 real product cards (live data, badges, prices), no console errors. Also confirmed the pre-existing card→item-modal click behavior still works (unrelated to this change) — via direct component-state inspection, after discovering this Chrome tab's screenshot output doesn't match its real DOM coordinate space (see updated Open Issue 5). Committed as DEC-016.

**2026-08-15 session (continued, redesign section 5 — Autumn/promo banner):** Restyled the existing admin-managed promotions banner to match the mockup's "Autumn Limited Edition" card, but kept it fully data-driven — asked Leo how to handle the mockup's feature-highlight bullets and specific seasonal copy (no backing field on `Promotion`); he chose restyle-only, drop the bullets, rather than extending the data model or hardcoding a disconnected static banner. Added a static "🍂 A Touch of Magic 🍂" section eyebrow and a generic "🎁 Limited Time Offer" pill tag (not tied to any specific promo's content), swapped the CTA to a higher-contrast cream pill button, kept the existing dark warm-brown card color (this app is a deliberate 3-color brand system per `styles.css`, not literally green like the mockup). Verified live in Chrome against the real seeded "10% OFF Iced Mana" promo: renders correctly, no console errors, confirmed the CTA actually router-navigates to `/shop?category=2` via a direct click + `location.pathname` check. Committed as DEC-017.

**2026-08-15 session (continued, redesign section 6 — testimonials):** Restyled the testimonials section to match the mockup's "Heartfelt Whispers" card structure (eyebrow, decorative quote-mark glyph, quote, name, role line), closing the backlog item that flagged the mockup's testimonials using actual Studio Ghibli character names — confirmed no `Testimonial` data model exists anywhere (frontend or backend), so, same as Philosophy (DEC-015), kept this as static copy but used the existing realistic customer names (Priya M./Daniel R./Ana L.) instead of the copyrighted character names, adding only a generic honest role line under each ("Neighborhood Regular", etc.) rather than the mockup's fictional in-universe titles. Verified live in Chrome: renders correctly, no console errors. Committed as DEC-018.

**2026-08-15 session (continued, redesign section 7 — Visit Us):** Added the "Visit Our Haven" 3-column location/hours/contact section, using real `StoreService` data (address, live open/hours status, real seeded phone) instead of the mockup's fictional address and invented weekly-hours summary — reused the same placeholder contact email already on the Contact page for consistency. Deliberately kept the hours row to "today's status + hours" (same pattern as the Hero and `LocationBannerComponent`) rather than building new logic to condense the full weekly schedule into a mockup-style summary line — no existing UI does that today. Verified live in Chrome against the real running backend: real address/phone/hours all render correctly, no console errors. Committed as DEC-019.

**2026-08-15 session (continued, redesign section 8 — footer, redesign complete):** Restyled the site-wide `FooterComponent` to the mockup's layout. Asked Leo how to handle the mockup's "Join Our Forest Mail" newsletter signup (no backing feature exists anywhere) — he chose skip it, not build a real subscriber feature or fake the form. With that slot open, also noticed the footer's existing "Visit Us" column (address/hours) had become fully redundant — confirmed `LocationBannerComponent` (top banner) and `FooterComponent` are both rendered once in `app.component.ts`, i.e. on every page, and the new home-page Visit Us section (DEC-019) covers it again — so removed that column and moved "Follow Along" (blurb + 3 social icons) into its place, keeping "Contact Us"/"Terms & Conditions" as a bottom bar next to the copyright line (matching the mockup's bottom-row pairing). No existing functionality was dropped, only the genuinely duplicated column. Verified via direct DOM/computed-style inspection rather than screenshots — this Chrome tab's screenshots stopped updating to reflect real page state mid-session (see updated Open Issue 5) — confirming correct grid/flex layout, all real content present, and "Contact Us" genuinely router-navigates to `/contact`. Committed as DEC-020. **This completes the Ghibli-themed home page redesign (DEC-013 through DEC-020) — every section in `home-cafe-by-bami-landing.png` has now been rebuilt.**

**2026-08-15 session (continued, full-site regression pass after the redesign):** Leo asked for a full pass to check for regressions from the 8-section redesign. Ran `ng build --configuration production` and found a real regression: the build hard-failed on the `anyComponentStyle` budget — `home-page.component.ts`'s inline styles had grown to 5.27kb across the redesign, over the old 4kb error ceiling. Raised the budget (2kb/4kb → 4kb/8kb, DEC-021) since this is legitimate content growth, not bloat — confirmed via `git show` this was never an issue pre-redesign. Backend compiles clean (untouched today). Verified home page and footer render correctly with no console errors across two full page-text/DOM sweeps. **Also discovered, unrelated to today's work:** `/about`, `/contact`, `/faq`, `/terms` (all `InfoPageComponent`) and `/shop` (`MenuPageComponent`) render blank on first paint — confirmed pre-existing (untouched since initial commit), confirmed real (reproduces in a clean production build, not a dev-server/HMR artifact), root-caused as far as "component's first change-detection pass after route navigation never runs" but not fully diagnosed — a first fix attempt (`ChangeDetectorRef.detectChanges()` in `ngOnInit`) didn't work and was reverted. Full write-up in `docs/docsdebug.md`. This is a significant, real bug (these pages have apparently never rendered content for real users) but is a separate, deeper investigation from the regression pass — flagged to Leo for a priority decision rather than continuing to sink time into it.

**2026-08-17 session (new Claude Code session, continuing from a large pile of uncommitted work — DEC-021 through DEC-035 — already sitting in the working tree from earlier the same day):** Picked up mid-checkout-redesign. Completed the remaining 3 phases of `docs/checkout-redesign-notes.md`:
- **Phase 2 (sticky 2-column layout):** Restructured `.checkout-grid` into a sticky 2-column grid at ≥900px — Order Review card, "Place Order" CTA, and trust line moved into a right-hand `<aside>` with `position: sticky`, single column below that. Verified live. Committed as `bca983c`.
- **Along the way, found and fixed a real bug:** hard-loading or refreshing `/checkout` directly always redirected to `/shop`, because `checkoutGuard` read `store.isOpen()` synchronously and that signal defaults to `false` until the async store-info fetch resolves. Fixed by adding `StoreService.ensureLoaded()` (resolves once the first fetch completes, or after a 5s safety timeout) and having the guard await it first. Verified: an empty cart still correctly redirects; a real cart on a hard `/checkout` load no longer bounces. Bundled into `bca983c`.
- **Phase 3 (structured PH address fields):** Leo chose Option B from §5 — 7 structured fields (House/Unit, Street, Subdivision, Barangay, City, Province, Zip, Delivery Notes), concatenated client-side into the same free-text search string the existing geocoding backend already expects, no backend change. An optional "Have a Google Maps link instead?" field preserves the old paste-a-link shortcut, taking precedence when filled. Verified live against the real backend: both the concatenated-fields quote and the Maps-link-override quote returned real fees. Committed as `4390e05`.
- **Phase 4 (promo codes, §6):** Built for real — new `PromoCode` backend entity (code, PERCENT/FIXED discount, active flag), a public `/api/v1/promo-codes/validate` preview endpoint, and full admin CRUD at `/admin/promo-codes` (mirrors the existing Promotions admin page, per Leo's choice of "full admin CRUD" over "seed a few codes only"). Checkout gets an Apply/Remove promo input in the Order Review sidebar; the discount is re-derived reactively client-side for the live preview but resolved authoritatively server-side again at `placeOrder()` — never trusted from the client (same principle as DEC-012's customization surcharges). Seeded `CHILL10` (10% off) — the code the homepage promo banner has advertised since `PromotionSeeder` was written. **Verified end-to-end via direct API calls**: validate endpoint, full admin CRUD lifecycle (create/toggle-inactive/delete), and a real placed order — ₱300 subtotal → 10% off → ₱270 discounted subtotal → ₱23.63 tax → ₱293.63 total — matching the frontend's own computed preview exactly. **Found and fixed a second real, unrelated, currently-live bug**: `order_items.gift_wrap` (added for DEC-028's gift-wrap feature) was silently missing from the actual H2 table — its NOT-NULL migration had failed on existing rows during a schema-update copy and left the column out entirely, breaking **all** order placement with a 500 error. Fixed via a direct `ALTER TABLE ORDER_ITEMS ADD COLUMN GIFT_WRAP BOOLEAN DEFAULT FALSE NOT NULL` through the H2 console — confirmed the column now exists and order placement works again. Not yet committed — see Files Changed.
- **Known limitation, not a new bug:** visually verifying the checkout promo UI and the new admin Promo Codes page live in the browser hit the same already-tracked Open Issue 12 symptom (routed/interaction-triggered content not re-rendering on this automated tab) — confirmed identical on the pre-existing `/admin/orders` route too when hard-loaded, so it's not specific to this session's new code. Business logic was verified instead via direct signal reads and API calls, which matched the server exactly. `ng.applyChanges()` itself also started throwing an internal assertion error tab-wide (even on `app-root`) partway through this session — a devtools-bridge issue, not an app defect.
- Backend dev server was found down at the start of this session (no shutdown log entry, likely killed) and was restarted twice (once at session start, once after the promo-code schema changes needed a fresh process to pick them up — `mvnw compile` alone doesn't restart an already-running `spring-boot:run`, a known gotcha from DEC-012).

**2026-08-18 session (new Claude Code session, recall + cleanup):** User asked where the prior session left off. This file's own narrative had stopped mid checkout-redesign (2026-08-17), but `git log` showed 4 more commits had landed since without ever updating this file (`e152c13` FAQ→Contact merge, `c2338c0` mobile UX pass, `c7a21bd` checkout overflow/pickup-time fix, `6ff8edd` surface real checkout errors — now the base for this session). On top of that, the working tree held a large uncommitted pile: `DECISIONS.md` entries DEC-022 through DEC-035 (write-ups for already-shipped work — Sourdough image, Philosophy placeholder image, section-width pass, `/shop` redesign, item-modal/cart-drawer rebuild, checkout Phase 1) plus ~20 modified/new files never committed at all.

Reviewed every uncommitted file's actual diff (not just the stat) before touching anything, which surfaced two real, previously-unknown problems: (1) `OrderService.placeOrder()` and `cart.service.ts` already read/wrote a `giftWrap` flag, but `OrderItem`/`OrderItemRequestDto`/`OrderItemResponseDto`/`CartItem` never declared the field — **the last several commits on `main` did not actually compile on a fresh clone.** (2) `item-modal.component.ts` and `cart-drawer.component.ts` (already committed in `c2338c0`/`c7a21bd`) reference `[panelClass]`/`[backdropClass]`/`[modalDecoration]` on `<app-modal>` and three image assets (`soot-sprite.png`, `vines-accent.png`, `wood-board-outer.png`) that were **never added to git** — the themed modal card and its decorations have been silently non-functional on any machine but the one that made them.

Split the pile into 14 logical commits (gift-wrap field fix first, as the most urgent), rather than one giant commit, so each is independently reviewable:
`47f4b38` fix gift-wrap compile break → `96ac087` per-IP order rate limiting (new, bucket4j) → `5d188c6` genuine-image validation + authenticated GCash receipt endpoint (new) → `cb8a361` Ice Level customization preset → `538e6c5` Sourdough image fix (DEC-022) → `3e54b9a` `/shop` grouped-by-category redesign (DEC-027) → `088c008` themed-modal support + its 3 missing assets (DEC-029/031/033) → `8bace38` delivery-map wood frame (DEC-035) → `c4664b6` location-banner removal (DEC-026) → `366a498` admin-panel-overview.md → `5308fee` remaining mockup reference images → `ed8aaec` Obsidian workspace state → `07bd25e` backfilled `DECISIONS.md` (DEC-022–038, including new entries for the 3 previously-undocumented fixes above) → `f7b2f88` gitignore the frontend dev-server log. Verified the end state compiles clean: `mvnw compile` (offline) and `ng build --configuration production` both succeeded with no errors (only the pre-existing CSS-budget warnings on home-page/checkout-page).

**Left deliberately untouched:** `1.md` at the project root — empty, untracked, purpose unclear; not added to git, not deleted. `.obsidian/workspace.json` — churns on every file open, left uncommitted as transient UI state.

**2026-08-19 session (new Claude Code session):** User asked what to prioritize next; recommended Open Issue 12 (the long-standing blank-first-paint bug) as the highest-impact item, since it was still the top unresolved risk after yesterday's cleanup. Picked up the concrete next step already flagged in `docs/docsdebug.md` from 2026-08-18: tested whether `eventCoalescing: true` in `app.config.ts` was the cause. Confirmed both the dev servers were already running (backend :8080, frontend :4200) and reproduced live via `claude-in-chrome`, using direct DOM/`ng.getComponent()` inspection rather than screenshots (per this project's own established workaround for this tooling's flakiness): a real `.click()` on a `routerLink` anchor navigated the URL but left the routed component's DOM stuck at shell-only size while its bound data was already correct, and it never self-healed after a 1.5s+ wait with no forced tick. Flipped `eventCoalescing` to `false`, and the identical repro — plus the separate cart-drawer signal case from the 2026-08-18 update — both painted immediately and correctly across repeated real-click navigations and a hard reload. **Open Issue 12 is now fixed** (DEC-039). Verified `ng build --configuration production` still succeeds clean. Updated `docs/docsdebug.md` (full investigation + fix write-up), `backlog.md` (checked off the escalated-scope item), and `DECISIONS.md` (DEC-039) to match. **Not yet committed** — see Files Changed below.

# Current Status

Backend confirmed via direct API calls (prior session): geocoding, address disambiguation, Lalamove quotation/dispatch all working end-to-end. Frontend/UI checkout-delivery flow now also confirmed this session, running locally (backend :8080, frontend :4200):
- Add item → Checkout → Delivery → address search → `POST /api/v1/delivery/quote` → 200, fee ₱95.00, resolved address + map rendered (Leaflet/OSM).
- Drag pin → "Confirm new location" → fresh `POST /api/v1/delivery/quote` → 200, fee updated to ₱98.00, map re-centered.
- Header/store name correctly shows "Home Cafe by Bami", confirming the DEC-004 seeder fix is live.

# Decisions Made

See `DECISIONS.md` for the full log — DEC-039 (Open Issue 12 fix) is the latest logged entry. DEC-038 (upload/receipt security hardening) was committed 2026-08-18 alongside DEC-022–037 (a mix of backfilled write-ups for already-shipped work and new entries for previously-undocumented fixes from this session). Note: the 2026-08-17 session's Phases 2–4 (sticky layout, structured address fields, promo codes) were deliberately **not** logged as DEC entries, per Leo's request to skip that paper trail that time — see `backlog.md` and the git history (`bca983c`, `4390e05`, `a091187`) instead.

# Files Changed (2026-08-15 session)

- `backend/src/main/java/com/bakery/order/OrderController.java` — public GET path variable renamed `orderNumber` → `publicToken`.
- `backend/src/main/java/com/bakery/order/OrderService.java` — `getOrderStatus` now looks up via `findByPublicToken`, returns `publicView()`; `placeOrder`'s response also switched `from()` → `publicView()`; `placeOrder` now rejects (`409`) a GCash order whose OCR-extracted reference already exists via `existsByGcashReference`.
- `frontend/src/app/core/models/order.model.ts` — added `publicToken` to the `Order` interface.
- `frontend/src/app/core/services/checkout.service.ts` — `getOrderStatus` documented as taking a `publicToken`.
- `frontend/.../checkout-page.component.ts`, `.../order-confirmation.component.ts`, `.../order-status-page.component.ts` — route/poll/"Track Order" link all switched from `order.id` to `order.publicToken`. Displayed "Order #…" text unchanged (still the human-readable `id`/orderNumber).
- `DECISIONS.md` (DEC-007, DEC-008), `backlog.md` (checked off the `existsByGcashReference` item) — updated to reflect the above.
- Verified live: restarted the file-backed H2 backend twice (no schema migration needed either time, `public_token` column already existed from `f1eb8c7`). First restart: confirmed old `orderNumber` public lookups now 404 while `publicToken` lookups resolve, confirmed a manually-set `gcashReference` is stripped from the public response but visible via the admin endpoint, and confirmed the full frontend flow (order confirmation → Track Order → status polling) in a real Chrome tab, checking actual network requests rather than trusting rendered DOM. Second restart (after adding the duplicate-reference check): reused an existing receipt image (same OCR'd reference as an already-placed order) → got the expected `409`; placed another order with a synthetically-generated receipt image carrying a fresh, never-seen reference number → got a normal `201`, proving the check doesn't false-positive block legitimate new references. All synthetic test orders created during this session's testing (`ORD-486426`, `ORD-702117`) were cancelled via the admin API afterward to keep the live orders board clean (the H2 DB is file-persistent now, per DEC-003, so it no longer clears itself on restart).

# Files Changed (2026-08-13 session)

- `backend/src/main/resources/application.yml` — H2 datasource switched from `jdbc:h2:mem:bakerydb` to `jdbc:h2:file:./data/bakerydb`; `app.ocr.tessdata-path` switched to `${TESSDATA_PATH:<old hardcoded Windows path>}`. **Committed and pushed** (`e483ef1`).
- `.gitignore` — added `backend/data/` (the new H2 file-DB directory). **Committed and pushed** (`e483ef1`).
- `DeliveryStatus.java` — added shared `fromLalamove(String)` parser.
- `OrderService.java` — added the ordering/terminal-state guard inside `applyDeliveryWebhookUpdate()`; added `syncDeliveryStatus(orderNumber)`; injected `LalamoveClient`.
- `LalamoveClient.java` — added `getOrder()` / `getDriver()`.
- `LalamoveOrderStatus.java`, `LalamoveDriver.java` — new records for the above.
- `LalamoveWebhookController.java` — removed its now-duplicate private `parseStatus()`, calls the shared one.
- `AdminOrderController.java` — added `PATCH /{orderNumber}/sync-delivery-status`.
- `admin-orders-board.component.ts` — added the "🔄 Refresh Delivery Status" button (`isDeliveryInProgress()` gates its visibility) and `syncing`/`syncDeliveryStatus()`.
- `admin-order.service.ts` — added `syncDeliveryStatus()`, same pattern as `dispatchDelivery()`.
- `docs/docsdebug.md` — new file (Leo's request) — running debug log, first entry is LALA-INTEG-044 with the full investigation + fix write-up.
- `DECISIONS.md`, `backlog.md`, `memory.md`, `PROJECT-CONTEXT.md` — updated to reflect DEC-003 (H2 persistence), DEC-005 (OCR path), and DEC-006 (sync fallback) as implemented.

Everything else above (DEC-006, the delivery-status sync work) is **not yet committed**.

- `backend/scripts/simulate-lalamove-webhook.js` — `--order-number` flag renamed `--public-token` (matches DEC-007's public-endpoint change); header comment and H2-console lookup query updated to select `public_token` alongside `order_number`. **Committed and pushed** (submodule `c7f70fd`, vault pointer bump `a243ab2`).
- `docs/lalamove-webhook-tunnel.md` — new how-to doc: symptom/diagnose/resolve steps for the Cloudflare quick-tunnel "Your Webhook URL is not responsive" failure, plus why quick-tunnel URLs aren't durable. **Committed and pushed** (submodule `3249f35`, vault pointer bump `ef5a64c`).
- `docs/docsdebug.md` — new entry: ORD-TRACK-089 investigation (root cause is the `order.status`/`deliveryStatus` sync gap, not WebSockets). **Committed and pushed** (submodule `75e55db`, vault pointer bump `e42e686`).
- No code changes for ORD-193396 — only a live data mutation via the admin API (`status: PREPARING → READY`), not a file change.
- `frontend/.../admin/services/admin-order.service.ts` — added `fetchOrders()` (returns the observable) alongside the existing `loadOrders()` (unchanged, still self-subscribes) so a polling timer can compose it with `switchMap`.
- `frontend/.../admin/orders/admin-orders-board.component.ts` — replaced `setInterval(loadOrders, 8000)` with `timer(0, 8000).pipe(switchMap(() => fetchOrders()))`; `pollSub: Subscription` replaces the old `timer?: ReturnType<typeof setInterval>` field.
- `frontend/.../order-status/order-status-page.component.ts` — replaced `setInterval(pollStatus, 6000)` with `timer(0, 6000).pipe(switchMap(() => getOrderStatus(token)), takeWhile(..., true))`, so polling stops on a terminal status via the RxJS chain instead of a manual `clearInterval` inside the response handler.
- `DECISIONS.md` (DEC-009), `backlog.md` (named-tunnel-durability item + auto-sync item added) — **committed and pushed** (submodule `fdd97f1`, vault pointer bump `22aee4c`), alongside the three frontend files above.
- `backend/src/main/java/com/bakery/order/OrderService.java` — added `syncOrderStatusFromDelivery(Order)`, called from `applyDeliveryWebhookUpdate()`, closing Open Issue 11.
- `DECISIONS.md` (DEC-010), `backlog.md` (checked off the auto-sync item, added a follow-up for the stepper's delivery-unaware labels), `docs/docsdebug.md` (ORD-TRACK-089 marked fixed). **Not yet committed** (this batch, alongside `OrderService.java` above).
- `frontend/.../shared/components/order-status-stepper/order-status-stepper.component.ts` — takes new `fulfillmentType`/`deliveryStatus` inputs; picks between the unchanged `PICKUP_STEPS` and a new `DELIVERY_STEPS` array; `currentIndex` splits the `READY` stage into "Rider Assigned"/"Picked Up" using `deliveryStatus`.
- `frontend/.../order-status/order-status-page.component.ts` — passes the new stepper inputs; added `isDeliveryFailed()` + a failure banner for Lalamove `REJECTED`/`CANCELED`; `statusMessage()` now delivery-aware for `READY`/`COMPLETED`.
- `frontend/.../checkout/order-confirmation/order-confirmation.component.ts` — passes the same new stepper inputs for consistency.
- `DECISIONS.md` (DEC-011), `backlog.md` (checked off the delivery-aware-stepper follow-up). **Not yet committed** (this batch, alongside the three frontend files above).
- `backend/.../catalog/CustomizationOptionCodec.java` (new), `CustomizationOptionDto.java` (new) — encode/decode a customization's priced option list (`"Oat:20"`), backward-compatible with the old bare-name format.
- `backend/.../catalog/CustomizationDto.java` — `options` now `List<CustomizationOptionDto>` instead of `List<String>`.
- `backend/.../catalog/ProductRequestDTO.java` — added `customizationPrices: Map<String, Map<String, BigDecimal>>`.
- `backend/.../catalog/ProductService.java` — `toCustomizations()` now bakes per-product prices into each preset's `optionsCsv`; added `PRESET_OPTION_NAMES`.
- `backend/.../order/OrderService.java` — added `resolveOptionsSurcharge()`, called from `placeOrder()`'s item loop; `unitPrice`/`lineTotal` now include the surcharge; rejects an invalid option with `400`.
- `frontend/.../core/models/product.model.ts` — new `CustomizationOption` interface (`{name, priceDelta}`), `Customization.options` retyped.
- `frontend/.../core/models/cart.model.ts` — `SelectedOption` gained `priceDelta`.
- `frontend/.../core/services/cart.service.ts` — `addItem`/`updateQuantity` now compute `unitPrice` including option deltas.
- `frontend/.../catalog/components/item-modal.component.ts` — shows priced options in the dropdown, Add-to-Cart total includes deltas, emits `priceDelta` per selected option.
- `frontend/.../admin/products/admin-product-form-modal.component.ts` — new per-option price inputs under each customization checkbox; `CUSTOMIZATION_PRESET_OPTIONS` mirrors the backend's preset names.
- `frontend/.../admin/services/admin-product.service.ts` — `ProductPayload` gained `customizationPrices`.
- `frontend/.../core/services/mock-data.ts` — updated the 3 shared customization constants to the new option shape.
- `DECISIONS.md` (DEC-012), `backlog.md` (checked off priced-customization item). **Not yet committed** (this batch, alongside all files above).
- `frontend/.../home/home-page.component.ts` — hero section rebuilt around `homepage_hero_video.mp4` (click-to-play, autoplay+muted+loop, mute toggle, live store address/hours overlay, repositioned "Order Now" tag doubling as the watermark cover). See DEC-013.
- `DECISIONS.md` (DEC-013). **Not yet committed** (this batch, alongside the component file above).
- `frontend/.../home/home-page.component.ts` — added the Welcome section, removed `.hero-bar` and its now-dead styles.
- `DECISIONS.md` (DEC-014). **Committed** (`476ecea`).
- `frontend/.../home/home-page.component.ts` — added the Philosophy section (eyebrow, heading/copy, stats row, image placeholder), fills in the `#philosophy` anchor.
- `DECISIONS.md` (DEC-015), `backlog.md` (Philosophy image asset gap, testimonial placeholder names). **Committed this session.**

Prior session: `backend/mvnw` — executable bit fix, committed and pushed as `4692a83`.

# Files Changed (2026-08-17 session — checkout redesign Phases 2–4)

- `frontend/.../checkout-page/checkout-page.component.ts` — sticky 2-column grid (Phase 2); structured PH address fields replacing the single free-text search field, plus an optional Maps-link override (Phase 3); promo code Apply/Remove UI in the Order Review sidebar, `appliedDiscount()`/`estimatedTax()`/`estimatedTotal()` computed signals (Phase 4).
- `frontend/.../core/guards/checkout.guard.ts`, `frontend/.../core/services/store.service.ts` — `ensureLoaded()` fix for the checkout-guard redirect race.
- `DECISIONS.md` — **not touched this session, deliberately** (Leo asked to skip the DECISIONS.md entry for the guard fix; no entries were added for Phases 2–4 either, so DEC-035 is still the latest logged decision despite all this work).
- `backlog.md` — checkout-guard bug logged then checked off; checkout redesign item updated to reflect Phases 2–4 done; promo-codes item checked off with the full write-up (including the gift-wrap schema fix). **Committed** (`bca983c` for Phase 2 + guard fix, `4390e05` for Phase 3).
- `docs/checkout-redesign-notes.md` — "Suggested next step" list updated as each phase completed; now shows all 4 done. **Committed** (`4390e05` for Phase 3's update; Phase 4's update not yet committed).
- **Phase 2 + guard fix committed and pushed**: submodule `bca983c` (`9adf6b2..bca983c`), vault pointer `f02bf78` (`c3485ce..f02bf78`).
- **Phase 3 committed and pushed**: submodule `4390e05` (`bca983c..4390e05`), vault pointer `edd25e3` (`f02bf78..edd25e3`).
- **Phase 4 (promo codes) — NOT yet committed:**
  - New package `backend/.../promocode/` — `PromoCode`, `DiscountType`, `PromoCodeRepository`, `PromoCodeDto`, `PromoCodeRequestDto`, `PromoValidationRequestDto`, `PromoValidationResponseDto`, `PromoCodeService`, `PromoCodeController` (public validate), `AdminPromoCodeController`, `ActiveRequest`, `PromoCodeSeeder` (seeds `CHILL10`, 10%).
  - `backend/.../config/SecurityConfig.java` — added `/api/v1/promo-codes/**` to the public permitAll list.
  - `backend/.../order/Order.java`, `OrderRequestDto.java`, `OrderResponseDto.java`, `OrderService.java` — `promoCode`/`discountAmount` fields; `placeOrder()` resolves the discount authoritatively via `PromoCodeService.validate()` before computing tax/total.
  - `frontend/.../core/models/promo-code.model.ts` (new), `frontend/.../core/services/promo-code.service.ts` (new) — checkout-time validate call.
  - `frontend/.../core/models/order.model.ts`, `frontend/.../core/services/checkout.service.ts` — `promoCode` threaded through `OrderRequest`/`Order` and the wire payload.
  - `frontend/.../features/admin/services/admin-promo-code.service.ts` (new), `frontend/.../features/admin/promo-codes/admin-promo-codes-page.component.ts` (new), `frontend/.../features/admin/promo-codes/admin-promo-code-form-modal.component.ts` (new) — admin CRUD, mirrors the Promotions admin page pattern.
  - `frontend/.../app.routes.ts`, `frontend/.../features/admin/admin-shell.component.ts` — new `/admin/promo-codes` route + sidebar link.
  - **Live H2 schema fix, not a file change**: `ALTER TABLE ORDER_ITEMS ADD COLUMN GIFT_WRAP BOOLEAN DEFAULT FALSE NOT NULL` run directly via the H2 console against the running dev database — fixes the missing-column bug that was breaking all order placement. This is a one-time live-DB fix, not something checked into a migration file (this project's H2 setup relies on `ddl-auto: update`, not versioned migrations).

# Open Issues

1. ~~Store address precision was reduced.~~ **Fixed.** `StoreSettingsSeeder.java` default coordinates are `14.8690823, 121.0430113`, matching the store's own Google Maps place link pin. `storeAddress` is the full postal address `"048 Kay Piskal Rd, Tigbe, Norzagaray, 3013 Bulacan, Philippines"` (matches `contact-page.component.ts`). Note: an earlier version of this fix set `storeAddress` to the bare place name `"Home Cafe by Bami"` — a code-review pass caught this as a regression (that field is shown to customers and sent to Lalamove as the pickup address, so a bare name loses needed street detail) — corrected same session. **Not yet re-verified live** — the running dev instance still has the bare-name value in memory (H2, existsById skips reseed); needs a backend restart to pick up the corrected seed.
2. ~~H2 in-memory DB wipes Store Settings on every backend restart~~ **Fixed (DEC-003, 2026-08-13).** Switched `application.yml` to `jdbc:h2:file:./data/bakerydb`. Verified live: set `storePhone` via the admin API, restarted the backend process twice, confirmed the value survived both times. `backend/data/` (the DB file) is git-ignored. Store phone/GCash info can now be entered once via Admin → Store Settings and will stick — still blocked on Open Issue 3 (Settings page render bug) actually letting that form load in some sessions.
3. **Admin → Store Settings page silently fails to render** after certain navigation paths — URL and nav shell are correct, but the settings form never appears and the underlying API call never even fires. **Attempted repro this session** (see Current Objective) — could not reproduce across ~8 navigation paths on this Windows machine with a clean login. Deferred to the Mac machine. Static code review of the component/service/routes/controller found nothing wrong, so this may be state-dependent (stale auth, a backend-restart race) rather than path-dependent, or may have been an artifact of `claude-in-chrome`'s own flakiness in the original report session (see item 5 below).
4. ~~`app.ocr.tessdata-path` in `application.yml` is hardcoded to a Windows path~~ **Fixed (DEC-005, 2026-08-13).** Now `${TESSDATA_PATH:C:/Users/Public.DESKTOP-3A1LBHM/...}` — override with a `TESSDATA_PATH` env var on any other machine (e.g. macOS Homebrew's `/opt/homebrew/share/tessdata`). Verified the Windows fallback still starts clean; **not yet verified with an actual macOS tessdata install.**
5. ~~Treat any "verified in browser" claim from automated browser tooling with suspicion~~ — this session's `claude-in-chrome` verification cross-checked actual `POST /api/v1/delivery/quote` network responses (not just rendered DOM) specifically to guard against the prior stale-DOM failure mode. Still prefer manual/network-verified checks over trusting rendered DOM alone for this flow. **New this session:** `claude-in-chrome` also became unreliable mid-session in a different way — screenshot capture timed out repeatedly (CDP `Page.captureScreenshot`) and clicks on radio buttons intermittently failed to register (e.g. the checkout Delivery radio silently not toggling) after a manual `resize_window` call. Root cause not confirmed (possibly a devicePixelRatio/viewport mismatch introduced by resizing), but treat single-click UI state changes as unconfirmed until verified by a follow-up screenshot or network call, not just "click succeeded." **2026-08-15, mechanism confirmed (menu-preview session):** coordinate-based clicks on home-page product cards silently missed every time — `getBoundingClientRect()` on the same cards showed real DOM positions consistent with a much larger viewport than the screenshot tool reported (screenshot said 1427×861; DOM/viewport math implied ~1713×1033). So this is a screenshot-vs-DOM coordinate scale drift specific to this Chrome automation tab, not an app bug and not random flakiness — when a click seems to do nothing, cross-check via `getBoundingClientRect()`/`ng.getComponent()` state inspection (or dispatch the interaction directly on the element/component) before concluding the app is broken. **2026-08-15, further degraded (footer session):** in a fresh tab this same session, `window.scrollTo()` executed fine (confirmed no JS error) but the `computer` screenshot tool kept returning the *same* stale frame regardless of scroll position or even a full page navigation — screenshots were unusable for verification entirely, not just coordinate-shifted. Fell back to `javascript_tool` DOM/computed-style inspection (`getBoundingClientRect`, `getComputedStyle`, direct `.click()` + `location.pathname` checks) to verify the footer redesign instead, which worked reliably. **Takeaway: when this tooling misbehaves, don't fight it — switch to DOM-level verification via `javascript_tool` rather than retrying screenshots/clicks.**
6. ~~"Call Lalamove Rider" (admin dispatch, order status Preparing) fails with the generic message "Could not get a delivery quote right now — please try again."~~ **Root-caused, fixed, and confirmed live.** Live reproduction (user pasted the backend's `[Lalamove]` stderr log, which was already being written server-side before this session) showed the real failure: `POST /v3/orders -> 422 ERR_INVALID_FIELD: '09605168262' is not valid 'phone'. Phone must be in e.164 format...`. This confirms the bug report's Root Cause #1 (payload validation), specifically the recipient phone — customers type their phone at checkout in ordinary PH local format (`09XXXXXXXXX`), which is stored/displayed that way everywhere, but Lalamove's `recipient.phone` strictly requires E.164 (`+639XXXXXXXXX`) and nothing normalized it before the `/v3/orders` call. **Fixed** in `DeliveryDispatchService.java`: added `toE164Philippines()`, which normalizes common PH input shapes to E.164 (returns null — dispatch now fails fast with a clear message — if the result isn't a plausible PH mobile number) and applies it only to the outbound Lalamove call; the stored `guestPhone` is untouched. Also fixed along the way: `LalamoveClient.friendlyMessage()` previously swallowed any Lalamove error id it didn't recognize into one generic string (bug report's Root Cause #4) — now falls back to a status-specific message (401/403 → credentials, 402 → balance, 400/422 → invalid request, 5xx → outage) when the specific id isn't mapped. **Confirmed live** after the user restarted the backend: re-tested end-to-end via direct API calls (store phone re-set to `+639171234567` — H2 had cleared it on restart, as expected per Open Issue 2 — got a real quote, placed test order `ORD-336213` with `guestPhone: "09605168262"`, the exact number from the original report, moved it to `PREPARING`, then dispatched). Result: `HTTP 200`, `deliveryStatus: ASSIGNING_DRIVER`, real Lalamove sandbox `trackingShareLink` returned. Test order lives only in in-memory H2 and disappears on next restart — no cleanup needed.

7. ~~"Mark Ready" is clickable before a Lalamove driver is assigned~~ **Fixed.** User reported orders stuck in `ASSIGNING_DRIVER` with no gate preventing "Mark Ready" before a driver is actually assigned, and proposed forcing driver assignment via a Lalamove sandbox "simulator" REST endpoint. Checked Lalamove's own API docs (`developers.lalamove.com`): **there is no `ASSIGNED` order status and no sandbox simulator endpoint to force driver assignment** — Lalamove's real order-status progression is `ASSIGNING_DRIVER → ON_GOING (driver accepted) → PICKED_UP → COMPLETED`, and "a driver is assigned" is signaled by a separate `DRIVER_ASSIGNED` webhook event populating driver name/phone/plate while the order can still show `ASSIGNING_DRIVER` overall — which `LalamoveWebhookController.java` already correctly implements. The real, valid gap was in `admin-orders-board.component.ts`: "Mark Ready" had **zero gating** — clickable regardless of delivery/driver status. **Fixed:** added `canMarkReady()` — pickup orders unaffected; delivery orders now require `driverName` set (from `DRIVER_ASSIGNED`) or `deliveryStatus` already `ON_GOING`/`PICKED_UP` before the button enables, with a hint shown while disabled. **Confirmed live this session** (see Open Issue 8) — the simulated webhook events flipped `driverName`/`deliveryStatus` exactly as this gate depends on.
8. **NEW — built and verified `backend/scripts/simulate-lalamove-webhook.js`**, a signed local webhook simulator (per Open Issue 7's `DRIVER_ASSIGNED`/`ORDER_STATUS_CHANGED` events), so driver assignment can be tested without waiting on Lalamove's sandbox to auto-match a driver. Building it surfaced and fixed a real signing bug in my own test harness (Git Bash mangles a leading-slash CLI arg like `/api/v1/lalamove/webhook` into a Windows path — worked around with `MSYS2_ARG_CONV_EXCL`, irrelevant to the actual Node script since its webhook path is a hardcoded JS constant, never a shell arg). Cross-verified the script's HMAC signature byte-for-byte against the real `LalamoveHmacSigner` Java class before trusting it live. **Fully confirmed against the real running backend and a real dispatched order** (`ORD-336213`, sandbox `lalamoveOrderId 3559074781054214793` — found via H2 console, since **`lalamoveOrderId` isn't exposed by any admin API/UI today**, added to `backlog.md`): ran `node scripts/simulate-lalamove-webhook.js 3559074781054214793 --full`, confirmed via `GET /api/v1/orders/ORD-336213` that `deliveryStatus` moved `ASSIGNING_DRIVER → ON_GOING` and `driverName`/`driverPhone`/`driverPlateNumber` populated — a real, signed webhook event accepted and applied by the backend, not just an HTTP 200 (the endpoint always returns 200 regardless of signature validity, so the order-state check was the actual proof). Usage and the H2-console lookup steps for `lalamoveOrderId` are documented in the script's header comment.
9. **NEW — added `--lifecycle` mode to `simulate-lalamove-webhook.js`** and ran the full realistic sequence the user manually walked through (assign → reject → reassign → pick up → complete) end-to-end. Sequence: `DRIVER_ASSIGNED` (driver A) → `DRIVER_ASSIGNED` (driver B, models "driver A rejected, B accepted" — Lalamove's docs don't expose a distinct "driver rejected" event, only `ORDER_STATUS_CHANGED`/`DRIVER_ASSIGNED`, so a rejection-then-reassignment is only ever visible as a second `DRIVER_ASSIGNED`) → `ORDER_STATUS_CHANGED ON_GOING` → `PICKED_UP` → `COMPLETED`. **Ran against a real user-placed order** (`ORD-182313`, not a synthetic test order) and confirmed via `GET /api/v1/orders/ORD-182313` after every single step — full state progression captured: `ASSIGNING_DRIVER/null` → `ASSIGNING_DRIVER/Juan Dela Cruz` → `ASSIGNING_DRIVER/Maria Santos` → `ON_GOING/Maria Santos` → `PICKED_UP/Maria Santos` → `COMPLETED/Maria Santos`. `ORD-833080` (the user's other real test order) was left untouched.
10. **Diagnosed why manually changing status via the Lalamove Partner Portal / sandbox tools didn't update the local live-orders board**: nothing in this codebase registers a webhook callback URL with Lalamove, and the backend runs on `localhost:8080` — Lalamove's cloud servers cannot reach it directly. Unless a public tunnel (ngrok, Cloudflare Tunnel, etc.) is set up and that URL is registered as the webhook endpoint in the Lalamove sandbox dashboard, portal-side status changes never reach `/api/v1/lalamove/webhook` and the local DB genuinely never updates — this is expected/by-design given the current setup, not a code bug. `simulate-lalamove-webhook.js` sidesteps this entirely by hitting the local endpoint directly. **Partially mitigated (DEC-006, 2026-08-13):** re-filed by Leo as bug LALA-INTEG-044 against a real stuck order (`ORD-179842`); reviewed his `PROPOSAL.md`, built the manual-sync half of it (`PATCH /api/v1/admin/orders/{orderNumber}/sync-delivery-status`, pulling `GET /v3/orders/{id}` + `/drivers/{driverId}` directly from Lalamove instead of waiting on the unreachable webhook), plus an ordering/terminal-state guard so this new pull path can't race the webhook and regress an order. Verified live against `ORD-179842` and the real sandbox: pulled the real `PICKED_UP` status + real driver info, idempotent on re-call, correctly ignored a simulated stale/out-of-order webhook, correctly applied a terminal `CANCELED`, then correctly refused to un-cancel it afterward. Frontend wired same session: "🔄 Refresh Delivery Status" button on `admin-orders-board.component.ts`, gated to only show on non-terminal deliveries. Verified live in Chrome — clicked it on `ORD-115748`, watched `ASSIGNING_DRIVER → ON_GOING` with real driver info land in the UI, and confirmed the button is correctly absent on the terminal `ORD-179842` card. **LALA-INTEG-044 closed.** Full write-up in `docs/docsdebug.md`.

**Tunnel now set up (2026-08-15, follow-up session):** started a Cloudflare quick tunnel (`cloudflared tunnel --url http://localhost:8080`), registered `https://millennium-infrared-barry-week.trycloudflare.com/api/v1/lalamove/webhook` in the Lalamove sandbox dashboard after the previously-registered tunnel URL had gone stale (its `cloudflared` process was no longer running, so the hostname stopped resolving entirely — Lalamove reported "Your Webhook URL is not responsive"). Verified the new tunnel routes through with a direct `POST` (got `200`). **Caveat:** quick-tunnel URLs are not stable — a new random URL is generated every time the tunnel restarts, and it must be re-registered with Lalamove each time. Full diagnose/resolve procedure documented in `docs/lalamove-webhook-tunnel.md`; tracked as a backlog item to eventually replace with a named (stable) Cloudflare tunnel.

11. ~~`order.status` (customer stepper) and `deliveryStatus` (Lalamove rider lifecycle) have no auto-sync (ORD-TRACK-089).~~ **Fixed (DEC-010, 2026-08-15).** `syncOrderStatusFromDelivery(Order)` in `OrderService.java`, called from `applyDeliveryWebhookUpdate()`, now auto-advances `order.status` to `READY`/`COMPLETED` for delivery orders as `deliveryStatus` progresses — same condition `canMarkReady()` already used for the manual button. Verified live via `simulate-lalamove-webhook.js` against a real order. **Still open, deferred (smaller, separate item):** the stepper's labels themselves remain counter-pickup wording (`Ready for Pickup`/`Picked Up`) even for delivery orders — see `backlog.md`.
12. ~~`/about`, `/contact`, `/faq`, `/terms`, and `/shop`'s product list render blank on first paint (also affected `/admin/orders`/`/admin/promo-codes`).~~ **Root-caused and fixed (2026-08-19).** Cause: `app.config.ts`'s `provideZoneChangeDetection({ eventCoalescing: true })` defers Angular's post-event change-detection flush to a later animation-frame/microtask boundary; when the triggering event handler itself kicks off something async (a router navigation, or a signal write whose zone-stable check resolves early), the deferred flush can end up scheduled against stale state and never actually fires — leaving the DOM stuck (component data was always correct, e.g. `ng.getComponent()` showed the right bound `html` string; only the DOM never painted it) until an unrelated zone event or a manually forced `ng.applyChanges()` happened to trigger the next real pass. Confirmed via a real (non-synthetic) `.click()` on a `routerLink` anchor plus a 1.5s+ wait with no forced tick — the DOM never self-healed, ruling out "just delayed." **Fix:** flipped `eventCoalescing` to `false` (reverts to Angular's default synchronous CD scheduling — the perf gain it provided is negligible at this app's scale). Verified: repeated client-side navigations across `/about`/`/contact`/`/shop`, a hard reload of `/faq`, and the cart-drawer signal case (Open Issue 12's 2026-08-18 update) all now paint immediately and correctly, no manual intervention needed. `ng build --configuration production` still succeeds clean. Full investigation and fix write-up in `docs/docsdebug.md`.
13. ~~Hard-loading or refreshing `/checkout` directly always redirects back to `/shop`.~~ **Fixed (2026-08-17).** `checkoutGuard` read `store.isOpen()` synchronously, which defaults to `false` until the async store-info fetch resolves — added `StoreService.ensureLoaded()` and had the guard await it first. Verified live both ways (valid cart no longer bounces; empty cart still correctly redirects).
14. **NEW — `order_items.gift_wrap` was silently missing from the live H2 table, breaking all order placement with a 500.** Found 2026-08-17 while testing promo codes. The column's NOT-NULL migration (added for DEC-028) had failed on existing rows during an H2 schema-update table copy, and the column was simply never added — Hibernate's entity mapping still expected it, so every `INSERT INTO order_items` failed. **Fixed** via a direct `ALTER TABLE ORDER_ITEMS ADD COLUMN GIFT_WRAP BOOLEAN DEFAULT FALSE NOT NULL` through the H2 console (a default lets existing rows backfill cleanly, unlike the original bare NOT-NULL add). Confirmed the column now exists and a real order placement (with a promo code) succeeded end-to-end afterward. **Not a migration file** — this project has no versioned migrations, only `ddl-auto: update`, so if this same failure recurs on a fresh clone or a teammate's machine, the same manual H2-console fix applies.

**2026-08-19 session (continued, "Track Your Order" feature — DEC-045):** Leo brought a UI/UX proposal for post-checkout flow + order tracking, plus web/mobile mockups (`frontend/src/assets/track_order_page/`). Reviewed both, flagged and resolved before building: a security gap (the mockup's single "order ref or phone" field would let a bare guessable order number work alone — Leo chose requiring both together), literal Studio Ghibli references needing genericizing per this project's established precedent, and a fabricated ETA line with no backing data. Used plan mode (2 research/design subagents) to scope the change before writing code. Built: a new public `POST /api/v1/orders/lookup` (order number + phone, own independent rate-limit bucket from checkout's), extracted the existing PH phone normalizer out of `DeliveryDispatchService` into a shared `PhoneNumberUtil` so lookup-time phone matching tolerates format differences, a new `/track-order` page with a client-side-only "Active Order Found" recall banner (`RecentOrdersService`, mirrors `CartService`'s localStorage pattern), merged `order-confirmation`'s unique content into the already-live `/order-status` page and deleted the now-redundant static page, and sourced a licensed replacement kitchen photo. Full technical write-up in `DECISIONS.md` (DEC-045). **Verified via direct API calls only** — placed a real test order, exercised every lookup case (correct/wrong-phone/wrong-order-number/reformatted-phone), confirmed the two rate-limit buckets are genuinely independent, and spot-checked admin login/list/mark-paid all still work; `mvnw compile` and `ng build --configuration production` both clean. **Not yet verified live in a browser** — the Chrome automation extension was disconnected this session. **Not yet committed** — see Files Changed below.

# Next Action

1. **Do a live browser walkthrough of "Track Your Order" (DEC-045)** before considering it done — search form validation/errors, the recall banner (including that it actually disappears once an order is marked complete via the admin board), and the merged `/order-status` page's new content (payment pill, items/total, driver-info box on a dispatched delivery order). Chrome automation was unavailable this session.
2. **Commit and push the "Track Your Order" feature** (DEC-045) — currently uncommitted; see Files Changed.
3. On the Mac machine: set `TESSDATA_PATH` env var to its local Homebrew tessdata path, and re-attempt reproducing Open Issue 3 (Settings page render bug) — it may reproduce there, or may already be resolved.
4. Once Open Issue 3 is resolved (or ruled out), re-enter store phone/GCash info in Admin → Store Settings — it will now actually persist across restarts (Open Issue 2 fixed).
5. Consider surfacing `lalamoveOrderId` on the admin order view (see `backlog.md`) so `simulate-lalamove-webhook.js` and the new sync endpoint don't require an H2-console lookup to test against a specific order.
6. ~~Decide priority on Open Issue 12.~~ **Fixed (2026-08-19)** — see Open Issue 12 above.
7. ~~Commit and push Phase 4 (promo codes) and the DEC-022–035 uncommitted pile.~~ **Done (2026-08-18).** All of it — Phase 4 plus the gift-wrap compile-break fix, rate limiting, upload/receipt security hardening, Ice Level preset, Sourdough fix, `/shop` redesign, themed-modal support + its missing assets, and the backfilled `DECISIONS.md` entries — is now committed and pushed to `origin/main` (`47f4b38`…`f7b2f88`), and the vault's submodule pointer was bumped and pushed too (`ddd9518`). Backend (`mvnw compile`) and frontend (`ng build --configuration production`) both verified clean.
8. **Live-verify the receipt-privacy change** (DEC-038) against the actual running dev backend — the admin board's receipt thumbnails now go through an authenticated blob fetch instead of a plain `<img src>`; this was verified by code review and a clean build, not a live click-through.
9. `1.md` (empty file at the project root) — left untouched; ask Leo what it's for before deleting it.
10. ~~Commit and push today's Open Issue 12 fix.~~ **Done (2026-08-19).**
11. Now that Open Issue 12 is fixed, the visual/click verification that was blocked by it (checkout promo UI, admin Promo Codes page, DEC-038's receipt fetch — see item 8 above) can finally be done via a real UI walkthrough instead of signal/API-only checks.
12. **Resume the paused delivery-quote-on-iPhone investigation** — real requests to `/api/v1/delivery/quote` (and even `/api/v1/store`) never once appeared in the mobile debug proxy's log during several real attempts on a real iPhone, despite the identical request succeeding from every PC-based test. Full write-up, what's ruled out, and exact next steps are in `docs/docsdebug.md`'s "Delivery quote fails on a real iPhone over LAN" entry. **Remember to check the phone's Wi-Fi proxy is set back to Off** if it wasn't already — Manual mode breaks the phone's other HTTPS traffic while active.
13. Related, smaller fix made along the way but not yet the main focus: `StoreService.loaded` now correctly means "real data arrived", not "an attempt finished" (DEC-044) — a failed fetch was previously flipping every "confirmed closed" check into a false-positive closed state, the same class of bug as DEC-041/042/043 one layer deeper.

# Risks

**Formerly top risk, now resolved (2026-08-19): Open Issue 12** — `/about`, `/contact`, `/faq`, `/terms`, `/shop`'s product list, `/admin/orders`, and `/admin/promo-codes` all rendering blank on first paint for real visitors and admins. Root-caused to `eventCoalescing: true` in `app.config.ts` and fixed by disabling it — see Open Issue 12 above and `docs/docsdebug.md` for the full investigation. **Also resolved earlier:** the `checkoutGuard` redirect race (Open Issue 13) and the missing `gift_wrap` column that was breaking all order placement (Open Issue 14) — the latter was silent and would have blocked every real customer order until someone happened to restart the backend and try to check out.

**2026-08-18 addition:** `main` had actually stopped compiling as of some earlier point in the 2026-08-17 work — `OrderItem`/`OrderItemRequestDto`/`OrderItemResponseDto`/`CartItem` were missing the `giftWrap` field their own call sites already used. Fixed (DEC-036) and confirmed both `mvnw compile` and `ng build --configuration production` succeed on the current `main`. All of today's commits are local only — not pushed to `origin`, and the vault's submodule pointer is still one commit behind (`879bd54`).

# Validation Status

Backend delivery flow: confirmed via direct curl, end to end (prior session). Frontend/UI checkout-delivery flow: confirmed via browser automation, network-response-verified (prior session). H2 file-persistence: confirmed live this session via a real restart-and-check cycle. OCR path change: startup-verified only (Windows fallback), not yet tested against a real macOS tessdata install. Delivery-status manual sync (DEC-006): confirmed live against the real Lalamove sandbox and a real order, including the ordering/terminal-state guard (see Open Issue 10 and `docs/docsdebug.md`).

**2026-08-17 additions:** Checkout sticky layout (Phase 2) and structured address fields (Phase 3): confirmed live in Chrome against the real backend, including a real geocoded quote and a real Maps-link-override quote. Promo codes (Phase 4): backend fully confirmed via direct API calls (validate, full admin CRUD, and a real placed order with correct discount/tax/total math); frontend logic confirmed via direct signal reads matching the backend exactly, but **visual/click verification in the browser was blocked by the pre-existing Open Issue 12 rendering bug** — not independently re-verified via a clean UI walkthrough, so a real click-through in an actual browser (not this automation tooling) is still worth doing before considering this fully done.

Replace and refresh this file each session. A full snapshot of the session this was distilled from is archived at `docs/history/handoffs/2026-08-11-mac-part4-handoff.md`.
